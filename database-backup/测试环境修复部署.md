# 测试环境修复部署指南

## 修复内容

### 1. ElasticSearch 迁移修复（3处修复）
**文件**: `lite-backend/migrations/init_elasticsearch.py`

**问题1**: 验证时错误地将索引名当作别名检查
**修复1**: 移除不存在的别名检查，只保留实际的2个别名

**问题2**: timestamp字段格式错误（浮点数秒 vs ES要求的毫秒整数）
**修复2**: 将 `time.time()` 改为 `int(time.time() * 1000)`

**问题3**: 索引名以 `.` 开头（ES未来版本将禁止）
**修复3**: 将 `.mat_migrations` 改为 `mat_migrations`

### 2. 模型配置网关客户端导入修复
**文件**: `lite-backend/main.py`
**问题**: 导入不存在的 `llm_config_gateway_client` 单例
**修复**: 改用 `get_llm_config_gateway_client()` 异步工厂函数

## 部署步骤

### 方式1: 仅替换 main.py（推荐）

```bash
# 1. 在测试服务器上备份当前文件
cd /path/to/lite-backend
cp main.py main.py.backup.$(date +%Y%m%d_%H%M%S)

# 2. 从本地复制修复后的文件到测试服务器
scp /Users/wxn/Desktop/NextAgentLite/lite-backend/main.py user@8.136.49.11:/path/to/lite-backend/

# 3. 重启后端服务
# 如果使用 PM2:
pm2 restart server

# 如果使用 systemd:
sudo systemctl restart mat-backend

# 如果直接运行:
# 先停止当前进程，然后
conda activate zzdsj-lite
python main.py
```

### 方式2: 完整更新（包含ES迁移修复）

```bash
# 1. 备份文件
cd /path/to/lite-backend
cp main.py main.py.backup.$(date +%Y%m%d_%H%M%S)
cp migrations/init_elasticsearch.py migrations/init_elasticsearch.py.backup.$(date +%Y%m%d_%H%M%S)

# 2. 复制修复后的文件
scp /Users/wxn/Desktop/NextAgentLite/lite-backend/main.py \
    user@8.136.49.11:/path/to/lite-backend/

scp /Users/wxn/Desktop/NextAgentLite/lite-backend/migrations/init_elasticsearch.py \
    user@8.136.49.11:/path/to/lite-backend/migrations/

# 3. 重启服务（同上）
```

## 验证修复

### 1. 检查模型配置加载

```bash
# 查看启动日志，应该看到：
# [OK] 已从llm-config-gateway拉取配置: providers=X, models=Y, aliases=Z
# 而不是：
# [WARN] 拉取统一模型配置失败: cannot import name 'llm_config_gateway_client'

tail -f logs/mat_qa_$(date +%Y-%m-%d).log | grep "llm-config-gateway"
```

### 2. 检查ElasticSearch迁移

```bash
# 查看启动日志，应该看到：
# ✓ 索引 document_chunks 存在
# ✓ 索引 general_vectors 存在
# ...
# ✓ 别名 chunks 存在
# ✓ 别名 cache 存在
# ✓ 索引验证成功
# 而不是：
# ✗ 别名 general_vectors 不存在
# ✗ 索引验证失败

tail -f logs/mat_qa_$(date +%Y-%m-%d).log | grep "索引验证"
```

### 3. API健康检查

```bash
curl http://8.136.49.11:8000/health
```

## 修复详情

### main.py 修改（第277-294行）

**修改前**:
```python
from service.llm_config_gateway_client import llm_config_gateway_client
from service.llm_unified_config_service import llm_unified_config_service
snapshot = await llm_config_gateway_client.fetch_config_snapshot()
```

**修改后**:
```python
from service.llm_config_gateway_client import get_llm_config_gateway_client
from service.llm_unified_config_service import llm_unified_config_service
client = await get_llm_config_gateway_client()

# 构建配置快照
providers = await client.list_providers()
models = await client.list_models()
defaults_dict = await client.get_defaults_simple()

snapshot = {
    "providers": [{"id": p.id, "name": p.name, "type": p.type, "base_url": p.base_url, "status": p.status} for p in providers],
    "models": [{"id": m.id, "model_id": m.model_id, "display_name": m.display_name, "model_type": m.model_type} for m in models],
    "aliases": [],
    "defaults": defaults_dict
}

logger.info(f"   [OK] 已从llm-config-gateway拉取配置: providers={len(snapshot.get('providers', []))}, models={len(snapshot.get('models', []))}, aliases={len(snapshot.get('aliases', []))}")
llm_unified_config_service.set_snapshot(snapshot)
```

**关键变更**:
1. 使用 `get_llm_config_gateway_client()` 异步工厂函数获取客户端
2. 调用 `list_providers()` 和 `list_models()` 获取数据
3. 使用 `get_defaults_simple()` 获取默认配置
4. 手动构建快照字典（不存在 `fetch_config_snapshot()` 方法）

### init_elasticsearch.py 修改

#### 修改1: 别名验证逻辑（第688-691行）

**修改前**:
```python
expected_aliases = [
    "chunks",
    "general_vectors",
    "domain_vectors",
    "papers",
    "documents",
    "cache",
    "media"
]
```

**修改后**:
```python
expected_aliases = [
    "chunks",
    "cache"
]
```

#### 修改2: timestamp格式（第86行）

**修改前**:
```python
"timestamp": time.time(),  # 浮点数秒，ES无法解析
```

**修改后**:
```python
"timestamp": int(time.time() * 1000),  # 转换为毫秒整数
```

#### 修改3: 索引名（第53、66、82行）

**修改前**:
```python
index=".mat_migrations"  # 以点开头，ES未来版本禁止
```

**修改后**:
```python
index="mat_migrations"  # 移除前导点
```

## 常见问题

### Q1: 服务重启后还是报同样的错误？
**A**: 检查是否真的替换了文件，可以查看文件修改时间：
```bash
ls -lh main.py
stat main.py
```

### Q2: conda环境未激活？
**A**: 确保使用正确的conda环境：
```bash
conda activate zzdsj-lite
which python  # 应该显示 conda 环境下的 python
```

### Q3: ElasticSearch迁移失败？
**A**:
1. 检查ES是否运行：`curl http://localhost:9200`
2. 检查ES版本是否支持：`curl http://localhost:9200 | jq .version.number`
3. 手动删除迁移记录重试：
```bash
curl -X DELETE http://localhost:9200/.mat_migrations/_doc/elasticsearch_version
```

## 回滚方案

如果修复后出现问题，可以快速回滚：

```bash
# 回滚 main.py
cp main.py.backup.YYYYMMDD_HHMMSS main.py

# 回滚 init_elasticsearch.py
cp migrations/init_elasticsearch.py.backup.YYYYMMDD_HHMMSS migrations/init_elasticsearch.py

# 重启服务
pm2 restart server
```

---

**修复时间**: 2025-10-11
**测试环境**: 8.136.49.11
**Conda环境**: zzdsj-lite
**影响范围**: 模型配置加载、ElasticSearch初始化
