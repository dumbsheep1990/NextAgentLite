# 2025-09-23 优化更新 — 问答路由与模板体系

本次更新围绕“知识库问答路由（Retrieval Paths）”与“路由模板（Templates）”进行了体系化改造，目标是：在同一知识库下配置多种检索路径组合，并将其沉淀为可复用的模板，以便在智能体构建时按“知识库 + 路由模板 + 检索模式（强制/平衡/自定义权重）”进行选择与执行。

---

## 概览
- 新布局（前端）
  - 问答路由页面改造为“知识库为主”的双栏视图：左侧知识库列表，右侧两张卡片（检索路径、路由模板）。
  - 新增“创建检索路径”弹窗，按字段创建单条路径；支持即时编辑与保存。
  - 新增“路由模板”卡片：支持“将当前路径保存为模板”“列出模板”“应用模板”“删除模板”。
- 服务层（后端）
  - retrieval_path_configs 初始化建表与索引；get/retrieval 返回“启用 + 禁用”路径集合。
  - 创建路径时自动避让 path_order 冲突；DB 返回的 config 统一反序列化为 dict。
  - 增加检索路由模板表 retrieval_path_templates 及 CRUD + 应用接口。
- 兼容性与容错
  - 资源端点 /qa-routing/resources/collections 不再依赖 metadata/vectorized_count 列；通过 join 文档统计数量。
  - 检索路径接口遇异常兜底为空数组，避免 500 影响前端体验。
  - 增加接口与服务层详细日志，便于问题定位。

---

## 前端优化（lite-qa）
- 新页面与路由
  - 新增：`lite-qa/src/pages/knowledge/QARoutingKBPage.tsx`
  - 路由替换：`lite-qa/src/routes/index.tsx` 指向新页面
- 交互与布局
  - 左侧知识库列表：优先调用 `/api/v1/qa-routing/resources/collections`，并对 `/api/v1/collections`（含分页与不分页）做返回格式兼容（数组/对象）。
  - 右侧“检索路径”卡片：
    - 表格列：顺序、来源（qa_routes/qa_datasets/documents）、启用开关、最小置信度、最大结果、回退动作、配置 JSON（查看/编辑）。
    - “新增路径”改为弹窗：字段包含路径名、顺序（可留空自动）、来源、回退动作、最小置信度、最大结果、启用、配置 JSON；创建成功自动刷新。
    - 修复高度铺满、仅表格区滚动、去圆角、左右顶部栏高度一致（56px）。
  - 右侧“路由模板”卡片：
    - “保存当前为模板”将当前路径集合落为模板（模板名、模式、可扩展权重）。
    - 列表展示模板（模板名/模式/路径数），支持“应用”“删除”。
- 样式
  - 新增：`lite-qa/src/pages/knowledge/qa-routing.css`（统一去圆角、头部高度、布局修饰）。
- 常见修复
  - 修复 placeholder 转义导致的 Vite 报错。
  - 修复内容区与卡片高度，避免出现整页滚动；仅列表区内部滚动。
  - 知识库列表不显示问题：多源兜底 + 返回格式兼容。

---

## 后端优化（lite-backend）
- 检索路径服务（`service/qa_routing_service.py`）
  - initialize：
    - 自动建表 retrieval_path_configs（IF NOT EXISTS）及索引。
    - 自动建表 retrieval_path_templates（IF NOT EXISTS）及索引。
  - get_retrieval_paths：返回“启用 + 禁用”所有路径（避免新建但未显示）。
  - create_retrieval_path_config：
    - 规范化枚举（source_type、fallback_action）入库。
    - 顺序冲突自动避让（取 MAX(path_order)+1 重新插入）。
    - config 统一反序列化为字典后再构造返回对象。
  - update_retrieval_path_config：动态更新字段时规范化枚举 + JSON 序列化。
  - 模板能力：
    - list_templates(kb)：返回指定知识库的模板列表。
    - save_current_as_template(kb, name, mode, weights)：将当前路径集合保存为模板（paths_json、weights_json）。
    - delete_template(template_id)：删除模板。
    - apply_template(template_id)：清空目标知识库路径后按模板 paths 写回（顺序规范化、字段规范化）。
- 检索路径与模板接口（`api/endpoints/qa_routing.py`）
  - 路由：
    - GET `/api/v1/qa-routing/knowledge-base/{kb_id}/retrieval-paths`
    - POST `/api/v1/qa-routing/retrieval-paths`
    - PUT `/api/v1/qa-routing/retrieval-paths/{path_id}`
  - 模板：
    - GET `/api/v1/qa-routing/knowledge-base/{kb_id}/templates`
    - POST `/api/v1/qa-routing/knowledge-base/{kb_id}/templates/save-from-current`
    - POST `/api/v1/qa-routing/templates/{template_id}/apply`
    - DELETE `/api/v1/qa-routing/templates/{template_id}`
  - 日志：创建/更新路径打印入参与异常堆栈，便于排错。
- 资源端点容错（`api/endpoints/qa_routing_advanced.py`）
  - GET `/api/v1/qa-routing/resources/collections` 改为 join knowledge_documents 统计 `document_count`，不再依赖 `metadata`/`vectorized_count` 列，避免 UndefinedColumnError。

---

## 数据库与迁移
- retrieval_path_configs（若不存在自动创建）
  - 字段：id、knowledge_base_id、path_name、path_order、source_type、is_enabled、config(JSONB)、fallback_action、min_confidence、max_results、created_at、updated_at
  - 索引：knowledge_base_id；(knowledge_base_id, path_order)
- retrieval_path_templates（新增，若不存在自动创建）
  - 字段：id、knowledge_base_id、template_name、mode、paths_json(JSONB)、weights_json(JSONB)、is_default、created_at、updated_at
  - 索引：knowledge_base_id

> 注：使用 IF NOT EXISTS 方式在服务初始化时自动建表，无需单独执行迁移脚本。

---

## 日志与排障
- 关键日志点：
  - `[QA-Routing] create_retrieval_path payload=...`
  - `[QA-Routing][DB] INSERT retrieval_path_configs ...`
  - 冲突自动避让：`path_order 冲突，改用新顺序=...`
  - 失败：`[QA-Routing][DB] 创建检索路径失败: ... | payload=...`（含堆栈）
- 常见问题：
  - 500：多为表未初始化/枚举未规范化/顺序唯一冲突；当前均已处理。
  - 新建后不显示：此前 get 仅返回启用项，现已改为返回全部。

---

## 使用说明（快速）
1. 打开“问答路由”：选择知识库 → 右侧“检索路径”/“路由模板”。
2. 新增路径：点击“新增路径”，在弹窗中按需配置（路径名/来源/阈值/结果/回退/启用/配置 JSON）。
3. 编辑路径：在表格行内直接调整顺序、开关、阈值等（自动保存）。
4. 保存为模板：点击“保存当前为模板”，输入模板名，稍后在模板列表即可见。
5. 应用模板：点击模板行“应用”将当前路径集合重置为模板定义。

---

## 待办与后续方向
- 模式/权重显式化：
  - 在“保存为模板”时支持选择模式（force/balanced/custom），自定义模式可编辑/保存权重；应用模板时用于检索聚合排序。
- 模板向导：
  - 提供“默认：QA+文档”“仅文档”“仅QA”“QA（自定义问答）+文档”等一键生成模板。
- 与智能体构建联动：
  - 在智能体创建页增加“选择知识库 + 路由模板 + 模式（含权重）”，执行时按模板路径集合完成检索。
- 更细粒度统计：
  - 路径命中/文档命中统计与近 3 次查询预览。

---

## 影响范围与兼容性
- 前端：问答路由页替换为新实现（原功能收敛至路径与模板两张卡片）。
- 后端：新增模板相关表与接口；检索路径接口改为返回启用+禁用集合；资源端点 SQL 做了兼容。
- 数据：未引入破坏性变更；初始化自动建表，不影响现有数据。

---

## 主要文件清单
- 前端
  - `lite-qa/src/pages/knowledge/QARoutingKBPage.tsx`
  - `lite-qa/src/pages/knowledge/qa-routing.css`
  - `lite-qa/src/routes/index.tsx`
- 后端
  - `lite-backend/service/qa_routing_service.py`
  - `lite-backend/api/endpoints/qa_routing.py`
  - `lite-backend/api/endpoints/qa_routing_advanced.py`
  - `lite-backend/models/qa_routing.py`（新增模板模型）

> 如需我继续实现“模板向导”“模式/权重编辑 UI”与“智能体构建页的模板选用”，可在此基础上直接追加迭代。

